@{
  Layout = "_Layout";
}
<div class="container">
  @await Html.PartialAsync("_Navbar")
  @model Factory.Models.Engineer
  <div class="card card-body">
    <h5 class="card-title">Editing: @Model.Name</h5>
    <form action="~/Engineer/Edit" method="post">
      @Html.HiddenFor(model => model.EngineerId)
      <div class="form-group">
        @Html.TextBoxFor(model => model.Name, new { @class="form-control required", @required="required", @placeholder="{Model.Name}"})
      </div>
      @if(ViewBag.Machines.Count != 0)
      {
      <div class="form-group">
        @Html.DropDownList("MachineId", (SelectList)ViewBag.MachineId, "Select machine", new { @class = "form-control" })
      </div>
      }
      <button type="submit" class="btn btn-primary">Update</button>
      <a>@Html.ActionLink("Cancel", "Index", "Engineer", null, new { @class="btn btn-danger"})</a>
    </form>
  </div>
  @await Html.PartialAsync("_Footer")
</div> 