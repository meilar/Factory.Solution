@{
  Layout = "_Layout";
}
<div class="container">
  @await Html.PartialAsync("_Navbar")
  @model Factory.Models.Machine
  <div class="card card-body">
    <h5 class="card-title">Editing: @Model.Name</h5>
    <form action="~/Machine/Edit" method="post">
      @Html.HiddenFor(model => model.MachineId)
      <div class="form-group">
        @Html.TextBoxFor(model => model.Name, new { @class="form-control required", @required="required", @placeholder="{Model.Name}"})
      </div>
      @if(ViewBag.Engineers.Count != 0)
      {
      <div class="form-group">
        @Html.DropDownList("EngineerId", (SelectList)ViewBag.EngineerId, "Select engineer", new { @class = "form-control" })
      </div>
      }
      <button type="submit" class="btn btn-primary">Update</button>
      <a>@Html.ActionLink("Cancel", "Index", "Machine", null, new { @class="btn btn-danger"})</a>
    </form>
  </div>
  @await Html.PartialAsync("_Footer")
</div> 